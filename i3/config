#Colors
set $brder-inactive		 #ffffff00
set $brder-active		 #2219aa52
set $bg-color 	         #2f343f
set $inactive-bg-color   #000000FF
set $text-color          #f3f4f5
set $inactive-text-color #00676E7D
set $urgent-bg-color     #E53935 

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# $mod = Windows key
set $mod Mod4

# Font for window titles
font pango:Droid Sans 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

floating_minimum_size 300 x 50
floating_maximum_size -1 x -1

# kill focused window
bindsym $mod+Shift+q kill

# new terminal
bindsym $mod+Return exec --no-startup-id urxvt

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
# to firefox
bindsym $mod+t [class="Firefox"] focus


# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+g split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh 

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

set $workspace1 "1"
set $workspace2 "2"
set $workspace3 "3"
set $workspace4 "4"
set $workspace5 "5"
set $workspace6 "6"
set $workspace7 "7"
set $workspace8 "8"
set $workspace9 "9"
set $workspace10 "10"
set $workspacei "11"
set $workspaceo "12"
set $workspacep "13"

# switch to workspace
bindsym $mod+1 workspace $workspace1, exec --no-startup-id bash /home/tho/bin/setwp 1, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+2 workspace $workspace2, exec --no-startup-id bash /home/tho/bin/setwp 2, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+3 workspace $workspace3, exec --no-startup-id bash /home/tho/bin/setwp 3, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+4 workspace $workspace4, exec --no-startup-id bash /home/tho/bin/setwp 4, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+5 workspace $workspace5, exec --no-startup-id bash /home/tho/bin/setwp 5, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+6 workspace $workspace6, exec --no-startup-id bash /home/tho/bin/setwp 6, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+7 workspace $workspace7, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh, exec --no-startup-id bash /home/tho/bin/setwp 7
bindsym $mod+8 workspace $workspace8, exec --no-startup-id bash /home/tho/bin/setwp 8, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+9 workspace $workspace9, exec --no-startup-id bash /home/tho/bin/setwp 9, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+0 workspace $workspace10, exec --no-startup-id bash /home/tho/bin/setwp 10, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+i workspace $workspacei, exec --no-startup-id bash /home/tho/bin/setwp 11, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+o workspace $workspaceo, exec --no-startup-id bash /home/tho/bin/setwp 12, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh
bindsym $mod+p workspace $workspacep, exec --no-startup-id bash /home/tho/bin/setwp 13, exec --no-startup-id bash /home/tho/.scripts/yabar_refresh.sh

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+9 move container to workspace $workspace9
bindsym $mod+Shift+0 move container to workspace $workspace10
bindsym $mod+Shift+i move container to workspace $workspacei
bindsym $mod+Shift+o move container to workspace $workspaceo
bindsym $mod+Shift+p move container to workspace $workspacep

assign [class="jetbrains-idea"] $workspacei

# Move the current workspace to the next output (monitor)
# (effectively toggles when you only have two outputs)
bindsym $mod+x move workspace to output left
bindsym $mod+Shift+x move workspace to output right

# no window borders
for_window [class="^.*"] border pixel 0

for_window [class="furxvt"] floating enable
for_window [instance="furxvt"] floating enable

# thobar
for_window [class="thobar"] floating enable; resize shrink height 10000px

#gaps
gaps inner 15 

#Gaps
set $mode_gaps Gaps: (o) outer, (i) inner
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

mode "$mode_gaps_inner" {
        bindsym plus  gaps inner current plus 5
        bindsym minus gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_outer" {
        bindsym plus  gaps outer current plus 5
        bindsym minus gaps outer current minus 5
        bindsym 0     gaps outer current set 0

        bindsym Shift+plus  gaps outer all plus 5
        bindsym Shift+minus gaps outer all minus 5
        bindsym Shift+0     gaps outer all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart, exec --no-startup-id pkill compton && compton -f & disown

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 5 px or 5 ppt
        bindsym k resize grow height 5 px or 5 ppt
        bindsym j resize shrink height 5 px or 5 ppt
        bindsym l resize grow width 5 px or 5 ppt


        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        bindsym Shift+h resize shrink width 40 px or 40 ppt
        bindsym Shift+k resize grow height 40 px or 40 ppt
        bindsym Shift+j resize shrink height 40 px or 40 ppt
        bindsym Shift+l resize grow width 40 px or 40 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# window colors
#                       border              background         text                 indicator
#client.focused          $brder-active       $bg-color          $text-color          #00ff00
#client.unfocused        $brder-inactive     $inactive-bg-color $inactive-text-color #00ff00
client.focused          $brder-active       $brder-active      $text-color          #00ff00
client.unfocused        $brder-inactive     $brder-inactive    $inactive-text-color #00ff00
client.focused_inactive $inactive-bg-color  $brder-inactive    $inactive-text-color #00ff00
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          #00ff00

